services:
  - type: web
    name: bess-model
    env: python
    buildCommand: "pip install -e ."
    startCommand: "gunicorn wsgi:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.2
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000
