services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - ./output:/app/output
      - ./config.example.yaml:/app/config.example.yaml
      - ./data:/app/data

